{
  "name": "ai-adoption",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "homepage": "https://tiagocarmo.github.io/ai-adoption",
  "scripts": {
    "build": "rm -rf public && mkdir -p public && cp index.html style.css script.js public/ && cp -R content public/content",
    "deploy": "gh-pages -d public"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
